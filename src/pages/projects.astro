---
import Layout from '@/layouts/default.astro'
import Card from '@/components/project_card.astro'
import { PROJECTS } from '@/data'
---

<Layout>
	<a href="/" text-sm text-gray-7 no-underline>&larr; Back</a>
  <h1 text-4xl>Random Stuff</h1>

  <p mb-10>
    Most of my projects are not finished yet, and some are still private on my GitHub :D
  </p>

  {PROJECTS.map((p) => (
    <div my-3>
      <Card
        name={p.name}
        desc={p.desc}
        repo={p.repo}
        web={p.web}
        doc={p.doc}
      />
    </div>
  ))}
</Layout>
